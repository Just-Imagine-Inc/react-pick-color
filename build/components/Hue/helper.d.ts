import { HslColor } from '../../types';
export declare const calculateHue: (e: any, hsl: HslColor, container: HTMLElement) => {
    h: number;
    s: number;
    l: number;
    a: number;
} | null;
